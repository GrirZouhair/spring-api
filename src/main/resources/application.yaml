spring:
  application:
    name: spring-boot
  datasource:
    url: jdbc:mysql://localhost:3306/spring_api?createDatabaseIfNotExist=true
    username: root
    password: "yes"
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}